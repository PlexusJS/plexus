{
  "name": "@plexusjs/core",
  "version": "1.0.1-canary.0",
  "main": "./dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc --build --force --verbose && echo \"Build complete.\"",
    "test": "jest",
    "prepublish": "yarn build"
  }
}
